# 文档目录（docs）

本目录用于存放**需求、设计、UI/UX**等文档类材料，**按当前 Git 分支分子目录**归档。

- 规则说明：`.cursor/rules/branch-docs-directory.mdc`
- 路径形式：`docs/<分支名>/`；分支名含 `/` 时对应多级子目录（如 `feature/foo` → `docs/feature/foo/`）。
- 建议在分支目录内使用：`requirements.md`、`design.md`、`ui-ux.md`。