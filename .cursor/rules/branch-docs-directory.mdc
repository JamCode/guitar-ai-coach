---
description: 按当前 Git 分支在 docs/<分支路径> 下存放需求、设计、UI/UX 等文档类材料
alwaysApply: true
---

# 分支文档目录规范

## 文档根目录

- 仓库根目录下使用 **`docs/`** 作为文档类材料的根目录（需求、设计、UI/UX 讨论与结论等）。
- **禁止**把上述文档默认写到项目代码目录深处或散乱路径；若用户**显式指定**其他路径，以用户当次指令为准。

## 与分支对应的子目录

- 每个**当前工作分支**对应**唯一**文档目录：`docs/<分支名映射>/`。
- **分支名映射规则**：
  - 分支名中的 **`/`** 原样对应为子路径。例如分支 `feature/chord-v2` → 目录 `docs/feature/chord-v2/`。
  - 分支名中的其他字符一般可直接用作路径段；若遇文件系统不允许的字符，改为连字符 `-` 并在文档内脚注真实分支名。
- **主分支**（如 `main`、`master`）同样适用：材料写入 `docs/main/` 等对应目录，避免与功能分支目录混用。

## 目录内建议文件（可按需增减）

在同一条分支目录下，优先使用以下**固定文件名**，便于检索与 `@` 引用：

| 类型 | 建议文件名 |
|------|------------|
| 需求（范围、验收、开放问题） | `requirements.md` |
| 设计（技术方案、架构要点、决策摘要） | `design.md` |
| UI/UX（导航、屏幕与状态、交互要点） | `ui-ux.md` |

- 若单类内容较多，可在该分支目录下再建子目录（如 `ui-ux/`），但**仍应保持该分支为顶层划分单位**。

## 对 Agent 的要求

1. **写入或更新**需求/设计/UI/UX 类文档前，应**以当前 Git 分支为准**确定目录（例如通过 `git branch --show-current` 或等价方式）；**不得**在未确认分支的情况下猜测路径。
2. 若处于 **detached HEAD** 或无法解析分支名，须**向用户确认**分支名或目标路径后再写入。
3. 目标目录若不存在，**创建目录后再写入文件**。
4. 会话中若已讨论需求/设计/UI/UX 且用户**希望落库**，应优先写入上述 **`docs/<分支映射>/`** 下的对应文件，而不是仅留在聊天里（除非用户明确说不要写仓库）。

## 与数据库 DDL 的关系（重要）

- **可执行的 schema 变更脚本**（`CREATE` / `ALTER` / `DROP`、索引 DDL 等）**不得**因本规则放入 `docs/`；须遵守 `database-ddl-policy.mdc`，**本期起**放在 **`backend/database/flyway/sql/V{版本}__描述.sql`**，由 **Flyway** 在部署时执行（见 `backend/database/flyway/README.md`）。**不要**把本期 DDL 只写在 `docs/` 或只追加到历史目录 `backend/database/ddl/`。
- **与分支相关的数据库说明**（改了哪些表/字段、回滚或数据注意点、对应迁移文件名）可写在 **`docs/<分支映射>/design.md`** 或 **`database-notes.md`**（仅叙述，**不替代** Flyway 下的正式脚本）。

## 与 skill 的关系

- 使用 `product-manager-workflow`、`mobile-ui-ux-workflow`、`software-architect-workflow`、`testing-strategy-workflow` 等产出**文档类内容**时，**默认**按本节路径归档；代码与测试实现仍遵循各 skill 及子项目惯例，**不**因本规则改变代码放置位置。
