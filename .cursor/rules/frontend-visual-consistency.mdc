---
description: 前端视觉一致性：颜色、字体、间距等与全局样式和设计体系对齐
globs: frontend/**/*.{vue,ts,css}
alwaysApply: false
---

# 前端视觉一致性

修改或新增前端界面时，须与现有视觉体系统一，避免同一产品内出现多套「自选配色 / 自选字体 / 自选圆角间距」。

## 颜色与主题

- 优先使用 `frontend/src/style.css` 中 `:root` 已定义的 CSS 变量（如 `--text`、`--text-h`、`--bg`、`--border`、`--accent`、`--accent-bg`、`--shadow` 等），以及暗色 `prefers-color-scheme: dark` 下同一套变量的覆写值。
- 若确需新增语义色或状态色，应**集中**在全局层扩展变量（例如在 `:root` 增加 `--success` / `--danger`），再在组件中引用变量；避免在单个组件里硬编码大量 `#rrggbb` 且无复用。
- 交互状态（hover、focus、disabled）应在同一组件或共享样式内成组定义，与现有强调色、透明度习惯保持一致。

## 字体与排版

- 正文与标题字体遵循全局：`var(--sans)`、`var(--heading)`、`var(--mono)`；不要随意引入新的 `font-family` 栈，除非经统一设计决策并写入全局样式。
- 字号、行高、字距尽量复用现有层级（全局 `h1`/`h2`、正文基准字号、响应式断点），避免同级标题在不同页面尺寸不一致。

## 间距、圆角与阴影

- 间距与圆角优先对照 `App.vue` 及各视图中已有区块的惯例，保持同一信息密度下的节奏一致。
- 阴影与边框优先使用 `--shadow`、`--border` 或与其同源的变量/写法，避免随意新写一整套 box-shadow。

## 组件与 scoped 样式

- Vue 组件内 `scoped` 样式可以覆盖细节，但**不应**另起一套与全局变量矛盾的主色或主字体。
- 新增通用样式若会在多处复用，优先考虑提到 `style.css` 或小型共享样式模块，而不是复制粘贴略有差异的片段。

## 实施检查（交付前自查）

- 新页面/新组件是否在亮暗色下都可读，且仍主要依赖全局变量？
- 是否在无必要情况下新增了第三种以上主色或第三套字体？
- 与相邻已有界面并排对比时，间距与标题层级是否看起来属于同一应用？
