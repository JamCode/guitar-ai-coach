---
description: 需求与开发交付模板规则（含标准需求与轻量变更）
alwaysApply: true
---

# 交付模板规则

在输出“需求评审结论、任务拆分、开发交付说明”时，优先按以下模板组织内容，保证信息完整且可执行。

## 标准需求交付模板

1. 需求目标与范围
   - 目标：
   - 本期范围（In Scope）：
   - 非本期范围（Out of Scope）：
2. 任务清单
   - 前端：
   - 后端：
   - 数据库（DDL）：
   - 数据初始化：
   - 测试与验收：
3. 原型与交互确认
   - 原型链接/说明：
   - 关键交互确认点：
4. 开发与联调
   - 前后端接口联调结论：
   - 数据库变更执行方式：
5. 交付结果
   - 已完成：
   - 未完成/延期：
   - 风险与后续建议：

## 轻量变更交付模板（小缺陷 / 小优化）

- 变更说明（改什么）：
- 变更原因（为什么现在改）：
- 影响范围（页面/API/数据）：
- 验证方式（如何确认生效）：
- 是否涉及数据库：
  - 若涉及，必须补齐 DDL 与初始化数据，并说明执行步骤。

## 必查约束

- 不得遗漏数据库 DDL 与初始化数据。
- schema 变更须使用 `backend/database/flyway/sql/V{版本}__描述.sql` 并通过 Flyway 执行；历史基线见 `backend/database/ddl/`（见 `database-ddl-policy.mdc`）。
- 应用运行时代码不得嵌入 DDL。
