---
description: 新功能交付须配套单元测试、API 测试与界面测试（含轻量变更例外）
alwaysApply: true
---

# 新功能测试交付要求

凡**本期新增或实质扩展的行为**（新接口、新页面/流程、新领域逻辑、新数据契约），在同一交付中须**同时**完成以下三类测试，并与功能代码一并提交或可运行：

1. **单元测试**  
   - 覆盖本次新增或修改的核心逻辑（纯函数、用例、服务层、校验与边界条件）。  
   - 放置位置遵循各子项目惯例（例如 Flutter 使用 `test/`，后端使用对应语言的 `tests/` 或项目约定目录）。

2. **API 测试**  
   - 凡暴露新的或变更的 HTTP/RPC 等对外契约，须有可自动执行的 API 层测试（如集成测试、契约测试或等价自动化），覆盖成功路径、主要错误码与鉴权/权限相关分支（若适用）。  
   - 不得仅以手工 Postman 截图代替可重复运行的测试资产，除非任务清单中已明确标注例外与替代验收方式。

3. **界面测试**  
   - 凡用户可见的新增或变更流程，须有界面层自动化验证：Flutter 可为 `integration_test`/Widget 级关键路径测试；Web 可为 E2E（如 Playwright）或项目既定 UI 测试栈。  
   - 应覆盖本条需求下的关键用户路径（含至少一种失败/空态/加载态若本期涉及）。

## 交付与验收口径

- **同一 PR/提交批次**中应能本地或 CI 执行上述测试；若 CI 尚未接入，须在任务说明中写明如何运行三条测试命令。  
- 若某类测试因环境依赖暂时无法自动化，须在任务清单或 PR 中**显式说明原因、补救计划与本期手工验收步骤**，不得静默省略。

## 轻量变更例外

以下情况可不强制三类齐全，但须在说明中写清「为何降级」：

- 纯文案、样式微调、配置开关默认值、日志级别等**无行为契约变化**的改动：至少保证不破坏现有测试；视情况只补必要单元或界面快照。  
- 紧急热修：可先合并最小验证，并在跟进项中补齐缺失测试类型并设定期限。

## 与需求流程的衔接

- 在任务清单中为新功能**单列**「单元测试 / API 测试 / 界面测试」三项，避免交付时遗漏。  
- 与 `new-requirement-workflow.mdc` 一致：**轻量变更**可走敏捷直做，但若轻量变更仍触及 API 或界面行为，须按上表**相应**补齐测试，而非一律免测。
