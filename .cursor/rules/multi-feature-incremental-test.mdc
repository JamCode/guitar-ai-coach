---
description: 多功能并行开发时按功能分批完成并逐批测试，避免积压到最后统一验收
alwaysApply: true
---

# 多功能交付：逐功能完成与测试

当**同一批次或同一会话**需要实现**多个相对独立的功能**时，优先采用**增量交付**节奏，而不是全部改完再一次性测试。

## 推荐顺序

1. **选定最小闭环**：从任务清单中拆出**一个**可独立验收的功能（或一个明确的子切片）。
2. **实现该功能**：完成该功能所需的代码与配置（含与本功能直接相关的最小联调面）。
3. **立即验证**：按 `new-feature-testing-requirement.mdc` 对该功能做**相应层级**的测试（至少覆盖本功能的核心路径与主要错误分支）；能跑自动化则本地或 CI 跑通后再进入下一项。
4. **再开始下一功能**：上一项验证通过（或明确记录已知问题与跟进项）后，再开下一功能的实现与测试。

## 避免的做法

- **堆到最后再测**：多个功能连续合并后才开始测试，容易交叉污染、定位困难、缺陷集中爆发。
- **无边界地并行大改**：多文件大面积未完成态并存时，难以判断失败来自哪一项变更。

## 与现有规则的关系

- 与 **新功能测试要求** 一致：每个功能仍须满足约定的单元 / API / 界面测试口径；本规则强调的是**时间顺序**与**批次粒度**。
- 若单功能本身已属「轻量变更」，仍可按 **new-requirement-workflow.mdc** 的轻量例外处理测试范围，但**仍建议**在该轻量项合并前完成与其行为相关的最小验证。

## Agent 执行口径

- 用户或任务清单标明「本期多项功能」时，Agent 应**主动按功能拆分**推进，并在每个功能节点提示或执行**对应测试**，再进入下一功能。
- 若用户明确要求「先全部实现再统一测」，可在说明取舍与风险后按其指示执行。
