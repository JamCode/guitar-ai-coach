---
description: 开始改代码前先拉取并合并当前分支对应的远程分支
alwaysApply: true
---

# 编码前先同步远程

在**开始编辑仓库代码之前**（含新开会话后首次动手改文件、或长时间未同步后继续开发），应先与**当前本地分支所跟踪的远程分支**对齐，避免在过时基线上开发或产生难合并的冲突。

## 推荐操作顺序

1. **确认分支**：`git status`，当前分支即本期开发分支；若尚未设置上游，先 `git push -u origin <branch>` 或按团队流程建立跟踪关系。  
2. **获取远程**：`git fetch origin`（或团队约定的 remote 名）。  
3. **合并远程跟踪分支**：  
   - 若已设置 upstream：`git pull`（默认会把 `origin/<当前分支>` 合并进当前分支），或显式 `git merge @{u}`。  
   - 若无 upstream 但明确对应远程分支：`git merge origin/<分支名>`。  
4. **处理冲突**：若有冲突，先解决并确认工作区干净后再开始写功能代码。  
5. **有本地未提交改动时**：先 `git stash` 再 pull/merge，合并完成后再 `git stash pop`；或先提交 WIP，再 pull/merge——**不要**在脏工作区上强行覆盖丢失改动。

## 例外与说明

- **新建分支且远程尚不存在**：首次推送前无需 pull；推送并设置 `-u` 之后，后续编码前仍应按本节同步。  
- **纯只读浏览**（不改文件、不跑会写仓库的生成脚本）：可不强制 pull；一旦要提交代码，仍应先同步。  
- 若团队统一使用 **rebase** 而非 merge 拉取（如 `git pull --rebase`），以团队 Git 规范为准，本规则的核心是：**动笔前先与远程对齐**，具体命令可与负责人确认后写进团队文档。

## 对 Agent 的要求

在 Cursor/Agent 将修改仓库时，若上下文显示可能长时间未同步，应**优先执行** fetch + pull（或 fetch + merge 跟踪分支），并在有冲突或异常时向用户说明，而不是直接基于陈旧 `HEAD` 大改。
