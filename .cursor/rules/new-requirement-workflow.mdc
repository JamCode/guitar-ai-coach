---
description: 新需求从讨论到开发的标准流程（含原型、任务清单、DDL 与初始化）
alwaysApply: true
---

# 新需求标准流程

处理任何“新增需求”时，统一遵循以下顺序，不可跳步：

1. 先讨论需求，明确业务目标、用户价值与边界，确定本期范围（做什么 / 不做什么）。
2. 基于已确认范围，产出可执行的任务清单（前端、后端、数据库、测试与上线准备）。
3. 先做前端原型（低保真或高保真均可），用于快速验证信息架构与页面结构。
4. 在原型基础上确认 UI 交互细节（状态变化、错误提示、空态、加载态、关键操作路径）。
5. UI 交互确认后再进入正式开发，开发必须同时参考：需求文档 + 任务清单 + 前端原型。

轻量变更例外（小缺陷 / 小优化）：

- 若变更范围小、影响面可控、无需重做信息架构或复杂交互评审，可走敏捷直做流程，不必完整走“讨论范围 -> 原型 -> 全量任务清单”。
- 直做前至少要补充一句范围说明（改什么、为何现在改、影响哪里），避免需求漂移。
- 若改动涉及数据库，仍必须遵守数据库规则：可执行 DDL 进入 `backend/database/flyway/sql/V{版本}__描述.sql` 并由 Flyway 应用，并补齐初始化数据；历史基线脚本在 `backend/database/ddl/`；不得在运行时代码嵌入 DDL。

开发阶段的强制要求：

- 前端、后端、数据库要协同实现，避免只完成单侧能力。
- 不得遗漏数据库 DDL 与初始化数据。
- 所有 schema 变更必须放在 `backend/database/flyway/sql/V{版本}__描述.sql`（Flyway），并提供可执行的初始化脚本或初始化 SQL；`backend/database/ddl/` 仅作历史基线参考。
- 应用运行时代码中不得嵌入 DDL；如 schema 未就绪，应返回明确可执行的错误提示。

交付前检查：

- 需求范围与最终实现一致，无超范围或漏范围。
- 任务清单项已逐项落地或明确标注延期原因。
- 前端实现与已确认交互一致，核心流程可演示。
- 数据库变更（DDL + 初始化）齐备，并可在目标环境独立执行。
