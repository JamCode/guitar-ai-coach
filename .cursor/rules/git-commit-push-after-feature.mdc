---
description: 功能开发与测试通过后须提交 Git 并 push 到当前远程分支
alwaysApply: true
---

# 功能完成后提交并推送远程

当**本期功能开发已完成**且**相关测试已执行通过**（参见 `new-feature-testing-requirement.mdc`）后，应**主动**完成本地提交并 **push** 到当前分支对应的远程，避免成果只留在本机。

## 执行条件（须同时满足再提交）

- 工作区改动已自检，**不提交**密钥、token、本机路径、大块生成物等不应入库内容。  
- **单元 / API / 界面**等本期约定测试已跑完且通过；若 CI 已配置，以 CI 绿灯为准或说明本地与 CI 差异。  
- 推送前若距上次同步较久，宜先按 `git-pull-before-coding.mdc` **fetch + pull/merge**，再提交、再 push，减少冲突。

## 推荐命令流程

1. `git status` 确认分支与待提交文件。  
2. `git add`（按需粒度，避免误加无关文件）。  
3. `git commit -m "..."`：说明**做了什么**（可带需求/任务编号，若团队有约定）。  
4. 已设 upstream：`git push`。  
5. 新分支首次推送：`git push -u origin <当前分支名>`。

## 对 Agent 的要求

在用户明确表示本功能与测试已结束、或上下文表明本期改动已就绪且测试已通过时，Agent 应**执行**上述提交与 push（在具备 shell/Git 权限的环境下），而不是只口头提醒用户自行操作。若 push 被拒（权限、保护分支、需 PR），应说明错误信息并给出下一步（如提 PR、找管理员开权限）。

## 例外

- 用户明确要求**暂不提交/仅本地保留**：尊重指令，不自动 push。  
- **WIP / 实验分支**且用户说明不必推送：可只 commit 不 push，或两者都不做，以用户当次说明为准。
