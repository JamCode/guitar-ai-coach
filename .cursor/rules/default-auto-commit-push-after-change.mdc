---
description: 默认在改动完成后直接 commit 并 push，除非用户明确要求先不提交
alwaysApply: true
---

# 默认自动提交与推送

当 Agent 完成当次开发改动并完成必要验证后，默认执行 **`git add` + `git commit` + `git push`**，不额外等待“请你提交”的二次确认。

## 执行口径

- 用户未给出相反指令时，按“默认自动提交并推送”执行。
- 用户明确说“先不要 commit/push”“先给我 review”“仅本地保留”时，必须暂停提交与推送。
- 提交范围只包含本次需求相关文件，避免夹带无关改动。
- 提交前保持既有安全约束：不提交密钥、token、凭证或其他敏感文件。

## 与现有规则的关系

- 仍需遵守 `git-pull-before-coding.mdc`：编辑前先同步远程。
- 仍需遵守测试与质量规则（如 `new-feature-testing-requirement.mdc`）。
- 若 push 失败（权限、保护分支、冲突等），需向用户报告并给出下一步建议。
