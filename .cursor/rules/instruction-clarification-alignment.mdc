---
description: 面对不明确指示时先反问与确认，与用户颗粒度对齐后再动手
alwaysApply: true
---

# 需求不明时的对齐方式

当用户的表述存在歧义、缺关键约束、或存在多种合理实现且取舍会影响结果时，**不要仅靠猜测默认推进**。应先以简短反问做确认，再按确认后的理解执行。

## 什么情况需要反问

- **范围不清**：改哪些文件/页面/API、是否包含迁移或回滚、是否要兼容旧数据或旧行为。  
- **优先级与取舍不清**：性能 vs 实现速度、最小可用 vs 完整方案、是否接受 breaking change。  
- **验收标准不清**：什么叫「完成」、期望的用户路径、错误形态与文案口径。  
- **颗粒度不清**：是要架构/方案讨论、只要伪代码、还是要直接改仓库并自测到某一步。

## 反问时怎么问

- 一次尽量聚焦 **1～3 个具体问题**，附带选项或简短举例，降低用户回复成本。  
- 可先 **复述自己的理解**（一两句话），问「是否指……若否则请纠正」，避免误会后大幅返工。  
- 若用户明确表示「你定」「按惯例」，可在项目既有规则与代码风格内合理默认，并在回复里 **简要说明采用的默认**，便于事后纠正。

## 与「轻量直做」的关系

- 小改动且风险面小时，反问可极短（一句确认），但仍应在 **真不确定** 时停下确认，而非静默假设。  
- 涉及数据库、对外接口、安全或删除数据的行为，只要有一点点不确定，优先确认后再改。
