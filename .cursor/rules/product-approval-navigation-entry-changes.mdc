---
description: 对用户可见的导航、Tab、工具列表等入口做隐藏、移除或迁出时须先经产品/负责人明确同意
alwaysApply: true
---

# 入口与导航变更须先确认

凡会改变用户**如何找到某项能力**的改动，在未得到**产品或仓库负责人（用户）明确同意**前，**不得**在当次实现中静默删除、隐藏或迁出相关入口。

## 须先确认的典型情形

- 从 Tab、侧边栏、首页卡片、工具列表等**移除**某项功能的导航链接或卡片。  
- 用「隐藏」「下线入口」「收敛到别处」等方式使用户**默认路径**无法到达原功能（即使代码仍保留）。  
- 将功能**仅**迁到其他 Tab/页面且**不在原位置**保留合理发现路径（例如无说明、无过渡入口），等效于弱删除的。  
- 将对外可见名称或分组改掉，导致用户按原习惯**找不到**等价能力（须按「信息架构变更」处理）。

## 可不经单独产品评审的情形（仍须在提交说明中写清原因）

- **缺陷修复**：恢复误删入口、修复死链、修正错误路由。  
- **用户当次指令**中已写明要删、要藏、要合并入口。  
- **纯文案/无障碍**修正且不改变可达的功能集合与主要路径。

## 对 Agent 的执行口径

- 若任务描述**未**包含「删除 / 隐藏 / 迁出某入口」，而实现上认为应当这么做：**先向用户简短确认**（说明影响面与替代路径），**收到明确同意后再改代码**。  
- 不得用「重构」「简化」「hide」等表述绕过确认；**用户可见范围收缩**一律按本节处理。  
- 与 `instruction-clarification-alignment.mdc` 一致：涉及**用户路径与功能发现**的取舍时，**优先反问**，而非默认替用户做决定。
