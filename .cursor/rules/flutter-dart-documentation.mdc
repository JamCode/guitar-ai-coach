---
description: Flutter/Dart 代码注释规范（至少到函数级，便于维护）
globs: flutter_app/**/*.dart
alwaysApply: false
---

# Flutter / Dart 注释要求

在修改或新增 `flutter_app/**/*.dart` 时，注释应**足够清晰**，方便后续快速理解职责与数据流。

## 必须做到

- **公开 API**（`class`、`mixin`、`extension`、`enum`、对外 `typedef`）：在声明上一行用 `///` 文档注释，说明**用途**；若有非显然的约束（线程、必须 `dispose`、必须主线程调用等）一并写出。
- **函数 / getter / setter / 工厂构造**：至少**函数级**说明「做什么」；若参数或返回值含义不直观，用 `@param` / `@returns` 或在正文一句话点明（Dart 风格下用自然语言简述即可，不强制 JavaDoc 标签）。
- **有副作用的行为**（改全局单例、播音频、请求权限、起异步长任务）：在注释中明确写出，避免误用。

## 建议做到

- 复杂算法、状态机、与平台或插件交互的边界：在关键分支或块前用简短 `//` 说明**为什么**这样写。
- `State` 类中若字段较多，可对**非显然**的成员变量用一行 `//` 说明生命周期或与其他字段的关系。

## 避免

- 重复代码字面意思的废话注释（如 `// i 加 1`）。
- 大段过时注释或与实现矛盾的注释；改逻辑时**同步更新**注释。
- 用注释掩盖命名问题；优先用清晰命名，注释补充「意图」而非替代码起名。

## 与「少改无关代码」的关系

本规则针对**本次触及或新增**的声明与逻辑：不要求一次性给整个历史文件补全注释，但在**修改某文件时**，应把**本次改动涉及的公开成员与关键函数**注释补到符合上述要求。
