# Deprecated

`frontend/` is deprecated and is no longer the primary client implementation.

- Active client development has moved to Swift/iOS code under `swift_app/` and `swift_ios_host/`.
- Do not add new features here unless explicitly required for legacy maintenance.

