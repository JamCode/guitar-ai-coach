# 复制为 install-device.local.env 后填写（该文件已加入 .gitignore，勿提交）
# Team ID：Xcode → Settings → Accounts → 选中 Apple ID → Team 一行括号内 10 位字符
export DEVELOPMENT_TEAM=XXXXXXXXXX
# 扒歌后端固定 App token：需与 ECS 上 backend.env 的 CHORD_ONNX_APP_TOKEN 一致
export CHORD_ONNX_APP_TOKEN=replace-with-your-token
