# 复制为 upload-testflight.local.env 后填写（该文件已在 .gitignore 中忽略）
# Team ID：Xcode -> Settings -> Accounts -> Team 名称后括号中的 10 位字符
export DEVELOPMENT_TEAM=7R8RS88G2M
export APP_BUNDLE_ID=com.wanghan.guitarhelper

# 扒歌服务 App 级固定 token；需与 ECS 上 CHORD_ONNX_APP_TOKEN 一致。
# 真实值不要提交到 Git。
export CHORD_ONNX_APP_TOKEN=replace-with-your-token

# App Store Connect API Key 信息（Users and Access -> Keys）
export ASC_KEY_ID=YOUR_KEY_ID
export ASC_ISSUER_ID=YOUR_ISSUER_ID

# AuthKey_<KEY_ID>.p8 的本机绝对路径（不要放仓库内）
export ASC_KEY_PATH="$HOME/Documents/certs/AuthKey_YOUR_KEY_ID.p8"

# 可选：显式指定营销版本（CFBundleShortVersionString）。不填则优先用工程值，缺失时脚本回退到 1.0.0。
# export MARKETING_VERSION_OVERRIDE=1.0.0

# 可选：显式指定本次归档 Build 号（不填则脚本默认用时间戳自动递增）
# export BUILD_NUMBER_OVERRIDE=2604161430
